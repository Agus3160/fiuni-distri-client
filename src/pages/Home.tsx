type Props = {};

export default function Home({}: Props) {
  return (
    <div className="bg-blue">
      <button type="button" className="btn btn-primary">
        Primary
      </button>
    </div>
  );
}
